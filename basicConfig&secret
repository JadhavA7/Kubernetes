

kubectl create secret generic mysecret --from-literal mysql-password=Pass@123
kubectl get secret
kubectl describe secret mysecret
kubectl get secret mysecret -o yaml


# secret-registry
kubectl create secret docker-registry mydockersecret --docker-username=dockerid --docker-password=(token) --docker-server=dockerhub.com --docker-email=
